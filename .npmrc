# React Native under pnpm workspaces needs hoisted node_modules so Metro can
# resolve transitive deps of expo + react-native packages (expo-modules-core,
# hoist-non-react-statics, etc.) without manually declaring every one of them.
# Without this, `expo export` / `expo start` fail mid-bundle with "Unable to
# resolve module X".
node-linker=hoisted
